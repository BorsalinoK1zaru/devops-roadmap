## День 2

Что сделал:
- Запустил Nginx в Docker.
- Проверил доступ через localhost:8080.
- Зашёл внутрь контейнера.

Что понял:
- Docker может запускать приложение в изолированной среде.
- Порт 8080 на моей машине ведёт к порту 80 внутри контейнера.

Что осталось непонятно:
- Отличие контейнера от виртуальной машины.
- Где находится HTML-файл Nginx.

Время:
- +-60 минут.

## День 3

Что сделал:
- Посмотрел локальные Docker images.
- Запустил Nginx со своей HTML-страницей.
- Проверил сайт через curl.

Что понял:
- Image и container — разные сущности.
- Через volume можно передать файлы с хоста в контейнер.
- Image — шаблон.
    Container — запущенный экземпляр шаблона.
    Nginx — веб-сервер, который отдаёт страницу по HTTP.

Что осталось непонятно:
- до конца не понимаю, что из себя представляет nginx

Время:
- 20 минут +-

## День 4

Что сделал:
- Создал свой `Dockerfile`.
- Собрал собственный image `my-nginx-site:day4`.
- Запустил контейнер из своего image.
- Проверил страницу через `curl` и браузер.

Что понял:
- Как писать Dockerfile
- Улучшилось понимание процесса сборки Docker image и запуска контейнера из него.

Что осталось непонятно:
- На данном этапе все понятно

Время:
- 40 минут

## День 5

Что сделал:
- Создал Dockerfile с несколькими инструкциями.
- Собрал image `my-nginx-site:day5`.
- Проверил повторную сборку и Docker cache.
- Изменил `index.html` и пересобрал image.
- Посмотрел историю image через `docker history`.

Что понял:
- Важен порядок выполнения команд в Dockerfile
- Docker image состоит из слоев
- При сборке образов используется кэширование, для ускорения сборок

Что осталось непонятно:
- Вроде как все понятно

Время:
- 20 минут

## День 6

Что сделал:
- Создал простое Flask-приложение.
- Создал `requirements.txt`.
- Написал Dockerfile для Python-приложения.
- Собрал image `python-web-app:day6`.
- Запустил контейнер и проверил `/` и `/health`.

Что понял:
- Стал лучше понимать архитектуру сервисных приложений в Backend
- Как происходит установка зависимостей внутри образа

Что осталось непонятно:
- В логах Gunicorn появился WORKER TIMEOUT. Приложение продолжило работать, но нужно позже подробнее разобраться с worker timeout и обработкой HTTP-запросов.

Время:
- 30

## День 7

Что сделал:
- Доработал Flask-приложение.
- Добавил переменные окружения `APP_NAME`, `APP_VERSION`, `APP_ENV`.
- Добавил endpoint `/version`.
- Собрал image `python-web-app:day7`.
- Запустил контейнер с переменными окружения через `-e`.
- Проверил приложение на порту `5050`.

Что понял:
- Для чего нужны переменные окружения в Docker
- Как добавлять переменные окружения при запуске контейнера
- Как добавлять их в код Python

Что осталось непонятно:
- Вроде бы понятно все

Время:
- 30

## Самостоятельная работа 1

Что сделал:
- Сделал собственное Backend приложение и запустил его внутри контейнера

Что получилось:
- Все получилось

Где были сложности:
- В общем нигде

Время:
- 20 минут

## День 8

Что сделал:
- Создал Flask-приложение, которое подключается к Redis.
- Создал Docker network `devops-net`.
- Запустил Redis в отдельном контейнере.
- Запустил Python-приложение в той же сети.
- Проверил `/redis-check` и `/counter`.

Что понял:
- понял суть взаимодействия контейнеров друг с другом, внутри wsl

Что осталось непонятно:
- на данный момент все понятно

Время:
- 30

## День 9

Что сделал:
- Создал Docker volume `redis-data`.
- Запустил Redis с подключённым volume.
- Проверил счётчик через Python-приложение.
- Удалил Redis container и создал его заново с тем же volume.
- Проверил сохранение данных Redis.

Что понял:
- Как работать с Volume, и впринципе, что это такое

Что осталось непонятно:
- Я до конца не понимаю, как из одного app.py мы запускаем 2 контейнера

Время:
- 20

## День 10

Что сделал:
- Создал проект с `compose.yaml`.
- Описал два сервиса: `app` и `redis`.
- Запустил весь проект через `docker compose up -d`.
- Проверил связь app → Redis.
- Проверил `/counter`, logs, network и volume.

Что понял:
- Что такое Docker compose и как это упрощает работу с контейнерами 

Что осталось непонятно:
- На данный момент все понятно

Время:
- 30

## День 11

Что сделал:
- Создал Compose-проект с `.env`.
- Вынес настройки приложения в `.env`.
- Добавил `env_file`.
- Добавил healthcheck для Redis.
- Настроил `depends_on` с `condition: service_healthy`.
- Проверил `/config`, `/redis-check` и `/counter`.

Что понял:
- Понял, как оптимизировать пространство в compose с помощью  .env

Что осталось непонятно:
- на данном этапе все понятно

Время:
- 20

## День 12. Самостоятельная работа 2

Что сделал:
- Собственное приложение с автоматической сборкой и запуском через compose

Что получилось:
- Получилось в точности реализовать приложение из запроса по ТЗ

Где были сложности:
- Нигде

Что понял:
- Понял, как Docker compose упрощает запуск приложения

Время:
- 30 минут

## День 13

Что сделал:
- Добавил `.gitignore`.
- Убрал `.env` из Git.
- Добавил `.dockerignore`.
- Добавил `gunicorn.conf.py`.
- Обновил Dockerfile.
- Улучшил README.md.

Что понял:
- Зачем нужен .gitignore
- Зачем нужен .dockerignore

Что осталось непонятно:
- Что такое juincorn и зачем оно нужно нашему приложению

Время:
- 20

## День 14

Что сделал:
- Проверил проект через `docker compose ps`.
- Посмотрел итоговую конфигурацию через `docker compose config`.
- Проверил логи app и redis.
- Зашёл внутрь контейнеров через `docker compose exec`.
- Проверил DNS service name `redis`.
- Намеренно сломал `REDIS_HOST`, увидел ошибку и исправил её.
- Добавил `restart: unless-stopped`.
- Создал `DEBUGGING.md`.

Что понял:
- Понял, как можно проверять работоспособность контейнеров
- Что делать в случае ошибок (например, если не подставились переменные из .env)

Что осталось непонятно:
- Вроде как все понятно

Время:
- 20

## День 15

Что сделал:
- Проверил использование диска Docker.
- Посмотрел images, containers и volumes.
- Создал дополнительный tag для image.
- Удалил tag.
- Очистил stopped containers.
- Очистил dangling images.
- Создал `CLEANUP.md`.

Что понял:
- Понял, как работать с занимаемой памятью внутри докера

Что осталось непонятно:
- Почему файлы CLEANUP и DEBUGGING я создал внутри task2

Время:
- 20
  
## День 16

Что сделал:
- Проверил текущий Dockerfile.
- Посмотрел пользователя внутри app-контейнера.
- Улучшил Dockerfile.
- Добавил запуск приложения от `appuser`.
- Пересобрал image без cache.
- Проверил endpoints.
- Проверил `docker compose exec app id`.
- Посмотрел историю image.
- Создал `DOCKERFILE_BEST_PRACTICES.md`.

Что понял:
- Как менять активного пользователя внутри контейнера

Что осталось непонятно:
- На данный момент все понятно

Время:
- 20 минут

## День 17

Что сделал:
- Обновил `compose.yaml`.
- Перешёл с `env_file` на `environment` и `${VARIABLE}`.
- Создал `.env.dev.example`.
- Создал `.env.prod.example`.
- Создал локальный `.env.dev`.
- Добавил debug-сервис `debug-redis-cli`.
- Запустил проект в dev-режиме.
- Запустил debug profile.
- Проверил Redis через debug-сервис.
- Проверил production config без запуска.
- Создал `COMPOSE_PROFILES.md`.

Что понял:
- Как разделять окружение на prod и dev
- Как отлаживать работу с разными средами

Что осталось непонятно:
- ...

Время:
- ...