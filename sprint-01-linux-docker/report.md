## День 2

Что сделал:
- Запустил Nginx в Docker.
- Проверил доступ через localhost:8080.
- Зашёл внутрь контейнера.

Что понял:
- Docker может запускать приложение в изолированной среде.
- Порт 8080 на моей машине ведёт к порту 80 внутри контейнера.

Что осталось непонятно:
- Отличие контейнера от виртуальной машины.
- Где находится HTML-файл Nginx.

Время:
- +-60 минут.

## День 3

Что сделал:
- Посмотрел локальные Docker images.
- Запустил Nginx со своей HTML-страницей.
- Проверил сайт через curl.

Что понял:
- Image и container — разные сущности.
- Через volume можно передать файлы с хоста в контейнер.
- Image — шаблон.
    Container — запущенный экземпляр шаблона.
    Nginx — веб-сервер, который отдаёт страницу по HTTP.

Что осталось непонятно:
- до конца не понимаю, что из себя представляет nginx

Время:
- 20 минут +-

## День 4

Что сделал:
- Создал свой `Dockerfile`.
- Собрал собственный image `my-nginx-site:day4`.
- Запустил контейнер из своего image.
- Проверил страницу через `curl` и браузер.

Что понял:
- Как писать Dockerfile
- Улучшилось понимание процесса сборки Docker image и запуска контейнера из него.

Что осталось непонятно:
- На данном этапе все понятно

Время:
- 40 минут

## День 5

Что сделал:
- Создал Dockerfile с несколькими инструкциями.
- Собрал image `my-nginx-site:day5`.
- Проверил повторную сборку и Docker cache.
- Изменил `index.html` и пересобрал image.
- Посмотрел историю image через `docker history`.

Что понял:
- Важен порядок выполнения команд в Dockerfile
- Docker image состоит из слоев
- При сборке образов используется кэширование, для ускорения сборок

Что осталось непонятно:
- Вроде как все понятно

Время:
- 20 минут

## День 6

Что сделал:
- Создал простое Flask-приложение.
- Создал `requirements.txt`.
- Написал Dockerfile для Python-приложения.
- Собрал image `python-web-app:day6`.
- Запустил контейнер и проверил `/` и `/health`.

Что понял:
- Стал лучше понимать архитектуру сервисных приложений в Backend
- Как происходит установка зависимостей внутри образа

Что осталось непонятно:
- В логах Gunicorn появился WORKER TIMEOUT. Приложение продолжило работать, но нужно позже подробнее разобраться с worker timeout и обработкой HTTP-запросов.

Время:
- 30

## День 7

Что сделал:
- Доработал Flask-приложение.
- Добавил переменные окружения `APP_NAME`, `APP_VERSION`, `APP_ENV`.
- Добавил endpoint `/version`.
- Собрал image `python-web-app:day7`.
- Запустил контейнер с переменными окружения через `-e`.
- Проверил приложение на порту `5050`.

Что понял:
- Для чего нужны переменные окружения в Docker
- Как добавлять переменные окружения при запуске контейнера
- Как добавлять их в код Python

Что осталось непонятно:
- Вроде бы понятно все

Время:
- 30

## Самостоятельная работа 1

Что сделал:
- Сделал собственное Backend приложение и запустил его внутри контейнера

Что получилось:
- Все получилось

Где были сложности:
- В общем нигде

Время:
- 20 минут