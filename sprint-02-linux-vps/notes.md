## День 21. Что я понял

- SSH нужен для подключения к удалённому Linux-серверу.
- Private key хранится локально и никому не передаётся.
- Public key можно добавлять на сервер или в панель VPS.
- chmod 600 нужен для приватного ключа.
- chmod 700 нужен для папки ~/.ssh.
- whoami, id и groups помогают понять текущего Linux-пользователя.

## День 22. Что я понял

- `ls -l` показывает права, владельца, группу и размер файла.
- `chmod` меняет права доступа.
- `chmod +x` делает скрипт исполняемым.
- `ps` показывает процессы.
- PID — идентификатор процесса.
- `kill PID` останавливает процесс.
- `ss -tulpn` показывает listening ports и процессы.
- `curl` помогает проверить HTTP-сервис.
- `tail -f` позволяет смотреть логи в реальном времени.
  

  ## День 23. Что я понял

- Bash-скрипт — это файл с командами терминала.
- Shebang `#!/usr/bin/env bash` указывает, чем запускать скрипт.
- `chmod +x` делает скрипт исполняемым.
- Exit code `0` означает успех, не `0` означает ошибку.
- `$1` — первый аргумент скрипта.
- `${1:-default}` задаёт значение по умолчанию.
- `set -euo pipefail` помогает писать более строгие скрипты.
- `curl -f` полезен для healthcheck.
- `ss -tulpn` помогает проверить, слушает ли сервис порт.
- `tail -n` показывает последние строки лога.

## День 24. Что я понял

- Bash-скрипты можно использовать для управления Docker Compose проектом.
- `compose-up.sh` запускает проект.
- `compose-down.sh` останавливает проект.
- `compose-status.sh` показывает состояние контейнеров и services.
- `compose-healthcheck.sh` проверяет HTTP endpoints приложения.
- `compose-logs.sh` показывает логи app или redis.
- `docker compose down` не удаляет volumes.
- `docker compose down -v` удаляет volumes и может удалить данные.
- Exit code `0` означает успешную проверку.
- Exit code `1` означает ошибку проверки.

## День 25. Что я понял

- VPS — это удалённый Linux-сервер.
- Для подключения нужны IP, user и SSH key.
- Public key можно добавлять на сервер.
- Private key нельзя коммитить или отправлять.
- `~/.ssh/config` упрощает SSH-подключение.
- В Git можно хранить только безопасный `config.example`.
- Baseline script помогает быстро проверить состояние сервера.
- Перед деплоем нужно проверить пользователя, ОС, диск, RAM, порты и интернет.

## День 27. Что я понял

- Docker Engine установлен на self-hosted Ubuntu server.
- Docker service управляется через systemd.
- Docker service должен быть active и enabled.
- `docker compose` работает как Docker CLI plugin.
- `hello-world` проверяет связь Docker CLI, Docker daemon, Docker Hub, image и container.
- Пользователь в группе docker может запускать Docker без sudo.
- Группа docker даёт высокий уровень доступа к серверу.
- Пользователь rustem тоже добавлен в docker group, потому что сервер используется совместно.
- Пока наружу открыт только SSH, порты приложений не открывались.